use std::io::BufRead;

use unnamed_language::parser::scanner::Scanner;

fn main() {
    for line in std::io::stdin().lock().lines() {
        let line = line.unwrap();
            }
        }
    }
}
